version: "3.9"
services:
  auth-postgres:
    environment:
      POSTGRES_DB: auth
      POSTGRES_PASSWORD: 123456
      POSTGRES_USER: auth_service
    image: "postgres:latest"
    volumes: 
      - /c/Users/Owner/prodev-conference-app/auth/data:/data/db
  auth:
    build: 
      context: ./auth
    depends_on:
      - auth-postgres
    environment:
      PGAPPNAME: conference_app
      PGDATABASE: auth
      PGHOST: auth-postgres
      PGPASSWORD: 123456
      PGPORT: 5432
      PGUSER: auth_service
    ports:
      - "8083:80"
    volumes:
      - /c/Users/Owner/prodev-conference-app/auth/data:/usr/local/auth/data
      - /c/Users/Owner/prodev-conference-app/auth/src:/usr/local/auth/src
  conference:
    build: 
      context: ./conference
    depends_on:
      - auth
      - locations
      - postgres
    environment:
      PGAPPNAME: conference_app
      PGDATABASE: conference_db
      PGHOST: postgres
      PGPASSWORD: 123456
      PGPORT: 5432
      PGUSER: conference_service
    ports:
      - "8080:80"
    volumes:
      - /c/Users/Owner/prodev-conference-app/conference/data:/usr/local/conference/data
      - /c/Users/Owner/prodev-conference-app/conference/src:/usr/local/conference/src
  frontend:
    build:
      context: ./frontend
    depends_on:
      - conference
      - locations
    environment:
      PORT: 80
      REACT_APP_API_URL: 'http://localhost:8080/api'
    ports:
      - "8081:80"
  locations-postgres:
    environment:
      POSTGRES_DB: locations
      POSTGRES_PASSWORD: 123456
      POSTGRES_USER: locations_service
    image: "postgres:latest"
    volumes: 
      - /c/Users/Owner/prodev-conference-app/locations/data:/data/db
  locations:
    build: 
      context: ./locations
    depends_on:
      - locations-postgres
    environment:
      PGAPPNAME: conference_app
      PGDATABASE: locations
      PGHOST: locations-postgres
      PGPASSWORD: 123456
      PGPORT: 5432
      PGUSER: locations_service
    ports:
      - "8082:80"
    volumes:
      - /c/Users/Owner/prodev-conference-app/locations/data:/usr/local/locations/data
      - /c/Users/Owner/prodev-conference-app/locations/src:/usr/local/locations/src
  postgres:
    container_name: postgres
    environment:
      POSTGRES_DB: conference_db
      POSTGRES_PASSWORD: 123456
      POSTGRES_USER: conference_service
    image: "postgres:latest"
    ports:
      - "5432:5432"
    volumes: 
      - /c/Users/Owner/prodev-conference-app/conference/data:/data/db
